import React from "react";

const Element = ({props}) => {
    return (
        <di>
            <h1> ID: { props.Id_producto } | Nombre: {props.Nombre} </h1>
        </di>
    );
};

export default Element;